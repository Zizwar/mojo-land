# Mojo Land v2.0 - Environment Variables Template

# ===========================================
# Supabase Configuration (Required)
# ===========================================
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_API_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# ===========================================
# OpenRouter Configuration (Required for AI)
# ===========================================
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key

# Optional: Default AI model to use
# Options: openai/gpt-4, openai/gpt-3.5-turbo, anthropic/claude-3-opus, etc.
DEFAULT_AI_MODEL=openai/gpt-3.5-turbo

# ===========================================
# Mojo Configuration (Optional)
# ===========================================
# Table name where endpoints are stored
MOJO_TABLE_NAME=mojos

# Token parameter name for authentication
MOJO_TOKEN_NAME=token

# URL parameter name for endpoint routing
MOJO_PARAM_NAME=land

# ===========================================
# Server Configuration (Optional)
# ===========================================
# Port for the server (default: 8000 for Deno, 3000 for Node.js)
PORT=8000

# Node environment (development, production, test)
NODE_ENV=development

# ===========================================
# Security (Optional)
# ===========================================
# JWT Secret (if using JWT authentication)
JWT_SECRET=your-jwt-secret-key-here

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ===========================================
# Logging (Optional)
# ===========================================
# Enable debug logging
DEBUG=true

# Log level (error, warn, info, debug)
LOG_LEVEL=info
